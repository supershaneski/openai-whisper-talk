{
  "name": "openai-whisper-talk",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "nuxt dev --host '0' --port 5000",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.0.0"
  },
  "dependencies": {
    "formidable": "^2.1.1",
    "openai": "^3.1.0"
  }
}
